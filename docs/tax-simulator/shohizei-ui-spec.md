# ②消費税シミュレーター 画面仕様書（U4・第1版）

作成日: 2026-08-29
前提: `ui-design.md`（基盤）・`hojinnari-ui-spec.md`（共通パターン）。本書は②固有の差分のみ。
対象: 仕様書 §14〜§22、②サービスの実装済み契約（適用可否判定＋方式比較＋§20輸出還付。返品・旧税率はエンジン第1版対象外）

## 1. 画面構成

URL `/tools/shohizei-simulator/`。

```
[STEP 1 事業者の状況（§15 判定の質問）] → [STEP 2 売上と仕入] → [結果]
```

- マウント直後からSTEP 1を表示し、導入画面・開始ボタン・所要時間表示は設けない。「入力と計算はこのブラウザ内で完結し、金額を保存・解析送信しません。」をSTEP 1上部に常設する
- 免税事業者と判定された場合は STEP 2 へ進まず結果（納税義務なし）へ直行してよい（サービスは免税を complete・比較なしで返す）
- ツール専用の純CSSヘッダ/簡素なフッタを使い、共通partialと `main.js` は使わない。ヒーローはSTEP 1では通常表示、STEP 2以降・計算中・停止・結果ではH1を残したまま `.simulator-intro--compact` で縮小しリードを隠す
- STEP・結果等への遷移はアプリ領域先頭へのスクロール後、`preventScroll` 付きで新画面見出しへフォーカスする。同一画面の入力エラーはエラー要約へのフォーカスを維持する

## 2. STEP 1 事業者の状況（§15）

| 画面項目 | 入力先 | 備考 |
| --- | --- | --- |
| 事業者の区分 | taxpayerType | 個人事業者／法人。3割特例（令和9・10年分・個人のみ）の判定に影響する旨は結果側で表示 |
| 課税期間 | context.consumptionTaxPeriod | 第1版は 2025年（暦年）固定。法人で事業年度が暦年と異なる場合は「対応準備中」の案内 |
| インボイス登録 | eligibility.invoiceRegistration.registered | 済／未／わからない。「済」で登録日（任意）と「登録を機に免税事業者から課税事業者になりましたか」（はい/いいえ/わからない） |
| 基準期間（前々年）の課税売上高 | eligibility.basePeriod | 輸出免税売上も含めた金額。無い場合は「基準期間がない（開業2年以内など）」チェック。法人は基準期間の月数も（12以外は年換算注記） |
| 特定期間（前年上半期）の課税売上高・給与等支払額 | eligibility.specifiedPeriod | 2欄。どちらかが1,000万円以下なら免税側判定になる旨を補足 |
| 簡易課税選択届出書 | filings（simplified_election） | 提出済み（効力開始年）／未提出／わからない |
| 課税事業者選択届出書 | filings（taxable_person_election） | 提出済み／未提出／わからない。提出済みは2割特例が個別確認（専門判定）になる旨を注記 |
| 該当の確認（まとめて1画面） | newCompany・events・taxablePeriodShortened・specialistChecks | 新設法人（資本金1,000万円以上等）／相続・合併・分割／高額な資産の取得／課税期間の短縮 — いずれか「該当・わからない」で専門判定の案内（§21） |

## 3. STEP 2 売上と仕入（§16・§17）

- 税込／税抜の切替は**欄ごと**（§16。全体一括にしない）。金額欄は共通部品
- 売上: 10%対象の課税売上／8%（軽減税率）対象の課税売上／輸出免税売上。輸出欄には「国内から商品を発送して海外へ販売した売上（eBay輸出など）。国外で完結する取引（不課税）は含めません」と表示し、輸出免税と不課税を区別する
- 仕入: インボイスあり課税仕入（10%／8%）／インボイスなし（免税事業者等から）の課税仕入 — 「ある」場合は 金額・その相手先の年間仕入合計（税込）・帳簿と請求書の保存の3点を1グループで入力（§18 の相手先別上限判定に必要な最小セット。判定できなければ一般課税行が確定にならないことを補足）
- 簡易課税の事業区分（簡易課税の届出がある場合のみ表示・§19）: **ユーザー選択**（自動推定しない）。例示付き文言:
  卸売業【第1種・みなし仕入率90%】／小売業（仕入れた物をそのまま売る。ネット物販含む）【第2種・80%】／製造業・建設業【第3種・70%】／飲食店業など【第4種・60%】／サービス業・コンサルタントなど【第5種・50%】／不動産業【第6種・40%】／複数の事業・わからない → 専門判定の案内（複数事業の特例計算は対応準備中）

## 4. 結果画面（§22）

1. **主役数値**: 結果見出し直下に「最も納税額が少ない方式（この試算では）」と、方式名＋納付額を大きな太字で表示する。一般課税が還付になる場合は「概算還付額（この試算では）」＋正の還付額を主役数値とする。免税の場合は「納税義務なし（免税事業者）」を同じ強調度で表示する
2. **利用できる方式**（判定リスト）: 4方式を○（利用可能）／×（対象外・理由）／？（入力不足・不足項目）／—（判定不能・専門判定）で表示。×の理由は文言で（例:「法人のため3割特例の対象外です」）
3. **納付・還付額の比較**: eligible かつ計算できた方式だけ金額を並べる（§22。対象外の方式に金額を並べない）。`totalPayable` の昇順とし、納付は正の金額、還付はマイナス記号だけに頼らず「還付 600,000円」のように表示する。各行に、一般課税は実額の仕入税額控除なので控除不足額が還付対象、簡易課税はみなし仕入率で算出するため還付なし、2割特例・3割特例は売上税額の所定割合を納付するため還付なし、という理由を表示する
4. **結論**: 「今回の入力条件では、**○○が最も納税額の少ない試算**となりました。」＋一般課税との差額（一般課税が確定できた場合のみ「▲○円」。確定できない場合は差額を出さない・§22）
5. 簡易課税が届出未提出で ineligible の場合: 「選択届出書を提出すると簡易課税を選べる可能性があります（提出期限は原則、適用したい課税期間の開始前日まで）」の案内
6. **免税の場合**: 通常は「基準期間・特定期間の状況から、納税義務がない（免税事業者）試算です。インボイス登録した場合の比較は、登録済みとして再計算してください」。輸出売上がある場合は「免税事業者は仕入税額の還付を受けられません。課税事業者を選択（インボイス登録等）した場合の還付可能性は、登録済みとして再計算してください」と案内する
7. 警告・前提・計算範囲・根拠・印刷・クリアは①④と同じ配置。主役数値は印刷対象に含める。前提には、還付額は概算であり、実際の還付申告では税務署の審査・還付加算金等により金額・時期が異なる場合がある旨を表示する。輸出売上は計算対象であり、除外項目にはしない

## 5. §20 輸出還付の計算表示

- 対象は一般課税・全額控除に限る。全額控除の利用者申告がfalseの場合や、個別対応方式・一括比例配分方式は従来どおりblockedとする
- 輸出免税売上は売上税額0円だが課税売上高へ含める。国内課税売上0円でも輸出免税売上があれば計算を続行できる
- 一般課税で仕入税額が売上税額を上回る場合、国税還付と地方還付を正の内訳で保持し、`totalPayable` は負値にする。国税還付には100円未満切捨てを適用せず、地方還付は正の国税還付額×22/78を円未満切捨てする
- 簡易課税・2割特例・3割特例は輸出免税売上を課税標準へ入れず、計算構造上還付は生じない

## 6. その他

- ①からの内部連携（consumptionTax.include）は既にサービス層で実装済み。②単体ページと①の詳細タブの接続は公開段階で調整（本書の範囲外）
- Analytics: mode イベントは送らない（②にモードなし）。他は共通
- 公開ページは生成しない（U2/U3 と同様）
