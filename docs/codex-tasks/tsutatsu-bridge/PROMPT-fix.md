前回の実装をレビューしたところ、1点だけ欠陥が見つかりました。修正をお願いします。

作業ツリーには前回の変更がそのまま残っています（コミットしていません）。その上で直してください。

## いまの状態

指示書を渡した直後に一度中断しています。そのため `scripts/lib/nta-tsutatsu.js` の
`findProvision` からは `commonRange` と `rangeStart` の**両方**が消えた状態です。
テストファイルも途中まで触られている可能性があります。

その後の実測で、**`rangeStart` は消しすぎ**だったと分かりました。今回はそこを戻します。

## 読む順

1. `docs/codex-tasks/tsutatsu-bridge/03-fix-range.md` — 今回の修正指示（これが主。実測値つきで書き直してあります）
2. 必要に応じて `01-bridge.md` の A-2

## 要点

- `commonRange`（`181〜223共-6` → `181-6`）は**誤り**。消したままにする。
  別の通達を「根拠」として記事に渡していました。
- `rangeStart`（`36-40～43` → `36-40`）は**正しい**。**戻すこと。**
  これは条文の範囲の左端で、引用されている通達そのものです。しかもこの解決は
  今回の変更前から効いていたので、消すと従来引けていたものが引けなくなり、
  `selfCheckContent` が正しい引用に捏造の警告を出します。
- 「共」の範囲表記は**検出はするが解決しない**（`found: false`）状態にする。
- テストを足す。いまのテストは「1件検出される」だけを見ており、すり替わっていても通ります。

期待する挙動と、追加してほしいテストの一覧は `03-fix-range.md` にあります。

A-1（連記）・B（橋渡し）・C（日次生成への組み込み）は良い出来でしたので、**触らないでください**。

## 補足

最初の誤りは、こちらの指示書（01-bridge.md の A-2）に「引けない場合はカタログ側の実際のキーを
調べて、そのキーに合わせること」と書いたことが原因です。`rangeStart` まで消させたのも
こちらの指示（前版の 03-fix-range.md）の誤りです。実装の判断ミスではありません。

## 完了条件

- `03-fix-range.md` のテスト追加を含め、`02-tests.md` の回帰確認がすべて通る
- `git status` が意図したファイルだけを示す

## 報告

- 何を削除し、何を戻したか
- 追加したテストが「すり替わり」をどう検出するか
- 修正後の実測値：`03-fix-range.md` の表の「あるべき姿」（検出 670 / 解決 659 / 範囲表記の解決 2件）と一致するか

**コミット・push はしないでください。** レビュー後にこちらで行います。
