あなたは日本の税理士事務所のブログ生成システム（Node.js）の実装担当です。

作業指示は `docs/codex-tasks/tsutatsu-bridge/` にあります。次の順に読んでから実装してください。

1. `README.md` — 背景・効果・守ってほしい前提
2. `01-bridge.md` — 実装（引用検出の穴2つ／橋渡し／日次生成への組み込み）
3. `02-tests.md` — テスト

## 進め方

- `01-bridge.md` の A → B → C の順に実装する
- **既存の部品を最大限使う。** `nta-tsutatsu.checkCitations` と `buildProvisionBlock` は既にあり、今回書くのは橋渡しと検出の穴埋めだけ
- **設計は先行実装に揃える。** `scripts/lib/law-sources.js` の `articlesFromSourceKankei` が同じ形の仕組みなので、必ず読んでから書く
- コメントは日本語。「なぜそうしたか」を書く。何をしているかの説明は不要
- 既存コードのコメント密度・命名・書き方に合わせる

## 禁止事項

- 記事の自動公開・自動マージに関わる変更
- `buildProvisionBlock` の【厳守】文言の書き換え
- 範囲表記の展開（`181～223共-6` から個別番号を作る）
- カタログに無い通達を渡すこと
- `content/posts/` 配下の記事ファイルの変更
- `assets/js/tax-simulator-boot.js` をコミットに含めること

## 完了条件

- `02-tests.md` の回帰確認がすべて通る
- `git status` が意図したファイルだけを示す

## 報告

作業が終わったら、次を簡潔に報告してください。

- 変更したファイルと、それぞれで何をしたか
- `01-bridge.md` の A-1 / A-2 をどう実装したか（正規表現をどう変えたか、打ち切り条件をどう書いたか）
- 実測値：質疑応答事例のうち通達が1件以上解決する件数（実装前は 517 件）
- 判断に迷った点と、その理由

**コミットや push はしないでください。** 変更を作業ツリーに残したままにしてください。レビュー後にこちらで行います。
