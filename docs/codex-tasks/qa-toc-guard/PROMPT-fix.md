ガードを入れたあとクローラを実行したところ、2点だけ直すところが見つかりました。

## いまの状態

前回の変更は作業ツリーにそのまま残っています（コミットしていません）。`data/` は元に戻してあります。

## 読む順

1. `docs/codex-tasks/qa-toc-guard/03-fix-cleanup.md` — 今回の修正指示（これが主）

## 要点

`node scripts/crawl-nta-qa.js --only keigen` の結果、**新しい混入は防げましたが、
既にある混入が残りました**。

**A. 既存が目次のときに守ってしまう**

`gaiyo-23` は既存の 822字 のほうが目次で、新しい 399字 が `【答】` を含む本文でした。
それを「短くなるから」と拒んで、**悪いほうを守っています**。

`shouldSkipBodyUpdate` は、既存の本文自体が品質チェック（答を含む・点線でない）を
通らないときは守らないでください。判断の軸は「短いかどうか」ではなく「壊れているかどうか」です。

**B. 取り込めなくなったファイルが孤児として残る**

`gaiyo-14`・`gaiyo-15`・`jirei-42` は分割結果に入らなくなり index からは外れましたが、
ファイルは残ったままです（index 134件に対しファイル139件）。

分割する資料について、その回に書き出さなかったファイルを消してください。
`--only` で指定した資料だけ、`--limit` 付きの実行では消さないこと。

なお `jirei-4`（731字→195字）と `jirei-8`（2169字→316字）の見送りは**正しい動作**です。
既存が本文で新しい方が目次でした。ここは変えないでください。

## やってはいけないこと

- A・B 以外に手を入れない。`splitByQuestion` のガードはそのまま
- `data/nta-qa/` を手で編集しない（クローラの実行はこちらでやります）

## 完了条件

- 03-fix-cleanup.md のテストを足し、02-tests.md の回帰確認がすべて通る
- `git status` が意図したファイルだけを示す（`data/` に差分が無いこと）

## 報告

- 「壊れている」の判定をどう書いたか（資料ごとに答の書き方が違う点をどう扱ったか）
- 孤児の削除がどの条件で走るか
- 追加したテスト

**コミット・push はしないでください。**
