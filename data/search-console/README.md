# data/search-console

サーチコンソールの週次取り込み先。中身の説明と設定手順は `docs/search-console-setup.md`。

- `latest.json` … 最新の取り込みの所在
- `report.md` … 週次レポート（管理画面 `/admin/analytics` に表示）
- `YYYYMMDD/` … 取り込み日ごとのデータ（直近12週分を保持）
